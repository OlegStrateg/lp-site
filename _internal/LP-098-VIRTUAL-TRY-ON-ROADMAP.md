# LP-098 — Virtual Try-On

Дата старта: 2026-09-13

Статус: IN PROGRESS

Рабочая ветка: `feat/LP-098-virtual-try-on-tool`

Точка восстановления: `recovery/pre-LP-098-virtual-try-on-2026-09-13`

База: `master@e982caffb8a53e0dfde135d0d7610cd9fd459a89`

## 1. Продуктовое решение

Virtual Try-On является отдельным инструментом сайта LayerPorter, а не конвертером и не отдельным сайтом.

Канонический путь инструмента: `/tools/virtual-try-on/`.

`/convert/` не изменяется: он остаётся каталогом конвертеров.

Архитектура должна позволять повторно использовать ядро в будущем Chrome-расширении без создания второго движка.

## 2. Принцип реализации

Два уровня качества:

1. `Instant Preview` — бесплатная локальная обработка в браузере. Никакого нашего GPU/API на каждую примерку.
2. `HD Try-On` — отдельный будущий генеративный слой, вызываемый только по явному действию пользователя. Не входит в первый MVP до проверки коммерчески чистого модельного стека.

Первый MVP намеренно ограничен верхней одеждой: футболки, рубашки, свитеры, куртки и похожие вещи. Нельзя выдавать геометрический preview за точный прогноз размера, посадки или физики ткани.

## 3. Open Source First

### Основной инженерный референс

`pravoobi/try-on` / `@practics/tryon-core`:

- framework-free ядро;
- MIT;
- on-device;
- pose estimation;
- person segmentation;
- garment warp;
- compositing;
- WebGPU/LiteRT подход.

Код демонстрационного приложения не копируется вслепую. Используем архитектурные решения и отдельно проверяем каждый переносимый компонент и лицензию.

### MVP runtime

Для первой изолированной версии используется MediaPipe Tasks Vision в браузере для определения позы. Версия runtime должна быть закреплена, модель — закреплена конкретным URL. Фото пользователя и вещи LayerPorter не отправляет на свой сервер.

MediaPipe может отправлять Google технические метрики производительности/использования; это должно быть явно сообщено пользователю до первого запуска.

### HD-контур

FASHN VTON 1.5 остаётся исследовательским кандидатом, но не включается в production, пока весь preprocessing/parser stack не очищен от некоммерческих зависимостей.

## 4. Три независимые проверки

### Проверка A — функциональная

`фото человека + фото вещи -> pose -> локальный composite -> результат -> скачать PNG`.

Проходят также повторная вещь и повторное фото без перезагрузки страницы.

### Проверка B — приватность и экономика

В обычном Instant Preview отсутствует наш upload endpoint и платный inference API. Изображения пользователя не должны уходить на сервер LayerPorter.

### Проверка C — регрессия сайта

`npm run build`, существующие smoke/verify/uniqueness проверки и новый LP-098 verifier проходят вместе. `/convert/`, существующие инструменты и production analytics не меняют поведение.

## 5. Дорожная карта

### Спринт 0 — границы и безопасность

- [x] Найти канонический репозиторий сайта.
- [x] Проверить `master`, HEAD, открытые PR и CI.
- [x] Создать recovery branch.
- [x] Создать изолированную feature branch.
- [x] Зафиксировать путь `/tools/virtual-try-on/`.
- [x] Зафиксировать MVP = tops only.
- [x] Не включать HD-модель до лицензионного gate.

### Спринт 1 — рабочий Instant Preview

- [ ] Страница инструмента на Astro/BaseLayout.
- [ ] Загрузка фото человека.
- [ ] Загрузка изображения вещи PNG/JPG/WebP.
- [ ] Локальное определение позы.
- [ ] Автоматическая подготовка предметного изображения.
- [ ] Геометрическое размещение вещи по плечам/торсу.
- [ ] Перерисовка рук поверх вещи для базовой окклюзии.
- [ ] Ручная коррекция масштаба и вертикального положения без повторного ML.
- [ ] Экспорт PNG.
- [ ] Ошибки: нет человека, не видны плечи/таз, плохой файл, runtime не загрузился.
- [ ] Явное privacy/runtime disclosure до запуска.

Gate S1: реальная примерка выполняется полностью в браузере на фронтальном фото без серверного inference.

### Спринт 2 — скорость и качество

- [ ] Кэшировать pose для одного фото пользователя.
- [ ] Не запускать ML повторно при изменении fit controls.
- [ ] Ограничить рабочее разрешение, не ухудшая preview.
- [ ] Измерять cold start / pose / render отдельно.
- [ ] Проверить WebGPU/CPU fallback.
- [ ] Сравнить результат с `@practics/tryon-core` TPS warp.
- [ ] Перейти на TPS, если прирост качества оправдывает вес/сложность.
- [ ] По возможности self-host runtime/model assets для стабильности и CSP.

Gate S2: повторная примерка той же фотографии существенно быстрее первой, UI не блокируется на повторном рендере.

### Спринт 3 — расширение категорий

Порядок только после метрик качества tops:

1. верх;
2. платья/цельные вещи;
3. низ;
4. многослойные образы.

Каждая категория проходит отдельный набор golden images и визуальный gate.

### Спринт 4 — Chrome Extension

- [ ] Вынести общий контракт try-on core.
- [ ] Product extractor из DOM карточки товара.
- [ ] Выбор корректного product image среди галереи.
- [ ] Кнопка `Try on`/Side Panel.
- [ ] Передача изображения товара в то же ядро.
- [ ] Локальный профиль фото пользователя.
- [ ] Не хранить секреты/API keys в расширении.

Gate S4: сайт и расширение используют одинаковый контракт и одинаковый движок обработки.

### Спринт 5 — HD Try-On

- [ ] Собрать benchmark dataset.
- [ ] Сравнить Google VTO с коммерчески чистыми open-source вариантами.
- [ ] Проверить лицензии кода, весов, parsers и preprocessors.
- [ ] Добавить HD только как опциональный второй уровень.
- [ ] Кэшировать person preprocessing.
- [ ] Не генерировать HD на каждый свайп.

### Спринт 6 — продуктовый слой

- [ ] Saved looks.
- [ ] Несколько пользовательских фото/ракурсов.
- [ ] Предварительно рассчитанная swipe-лента.
- [ ] Аналитика: upload -> run -> result -> download -> repeat -> HD -> outbound click.
- [ ] B2B widget/API только после подтверждения спроса.

## 6. Чек-лист перед PR

- [ ] Изменения изолированы LP-098.
- [ ] Нет изменений `/convert/`.
- [ ] Нет API keys/secrets.
- [ ] Runtime/model version pinned.
- [ ] Privacy disclosure соответствует фактическому поведению.
- [ ] Страница имеет `noindex` до живой проверки.
- [ ] Новый verifier проходит.
- [ ] Общий `npm run build` проходит.
- [ ] Desktop UI проверен.
- [ ] Mobile UI проверен.
- [ ] Повторная примерка работает без reload.
- [ ] Ошибка ML/runtime не ломает страницу.
- [ ] Production merge не выполняется до отдельного VERIFIED.

## 7. Критерий VERIFIED

Статус VERIFIED ставится только после реального запуска в Chrome на preview/deploy URL и проверки минимум трёх комбинаций `человек + вещь`, включая повторную примерку и скачивание результата.
